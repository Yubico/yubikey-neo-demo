NOTE: This project is deprecated and is no longer being maintained.

This is an example app showing how you can work with the NFC functionality
in the YubiKey NEO.

It uses Maven3 for building, so to build issue mvn3 package and you get an
apk in the target directory that can be installed on a phone with NFC
